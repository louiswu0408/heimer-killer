run "python --version" in pwsh to make sure you have python installed
pip install pygame
git clone it to local
run 
python "main (3).py"
(u can copy paste the script above and enter) under heimer killer
then u can play
use the arrow keys to move
space to flash
press esc to adjust the difficulty
press q to exit 
enter to have another one
have fun! :)
